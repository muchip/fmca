[build-system]
requires = ["scikit-build-core>=0.10", "pybind11>=2.12", "setuptools_scm>=8"]
build-backend = "scikit_build_core.build"

[project]
name = "fmca"
dynamic = ["version"]
description = "Fast Multiresolution Covariance Analysis"
readme = "README.md"
license = {text = "AGPL-3.0-only"}
requires-python = ">=3.9"
dependencies = ["numpy>=1.20"]

[project.urls]
Homepage = "https://github.com/muchip/fmca"

[tool.scikit-build]
minimum-version = "0.10"
cmake.version = ">=3.21"
cmake.build-type = "Release"
wheel.packages = []
sdist.exclude = ["py/*.ipynb", "build/", ".github/"]
metadata.version.provider = "scikit_build_core.metadata.setuptools_scm"

[tool.scikit-build.cmake.define]
FMCA_BUILD_TESTS = "OFF"

[tool.setuptools_scm]

[tool.cibuildwheel]
build-verbosity = 1
skip = ["pp*", "*-musllinux_*", "*_i686", "*-win32", "*t-*"]   # no PyPy, musl, 32-bit, free-threaded
test-command = "python -c \"import FMCA\""
build-frontend = "build"
 
[tool.cibuildwheel.linux]
# gcc in manylinux_2_28 ships libgomp; auditwheel bundles it. Eigen comes via FetchContent.
environment = { CMAKE_BUILD_PARALLEL_LEVEL = "4" }
 
[tool.cibuildwheel.macos]
before-all = "brew install libomp"
environment = { MACOSX_DEPLOYMENT_TARGET = "11.0", OpenMP_ROOT = "$(brew --prefix libomp)", CMAKE_BUILD_PARALLEL_LEVEL = "4" }
# delocate bundles libomp.dylib into the wheel
 
[tool.cibuildwheel.windows]
environment = { CMAKE_BUILD_PARALLEL_LEVEL = "4" }
